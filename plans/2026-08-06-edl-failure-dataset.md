# EDL select-failure dataset

Generated by script from the magenta thread archive (~2600 threads, 6 days): 31 zero-match failures across 29 tool calls. FAILED is the verbatim selector that matched nothing; WORKED is the verbatim selector from the agent's next script that reached the same place. Grouping is hand-assigned; all command text is extracted verbatim from the logs.

Tally: A 5 · B 4 · C 8 · D 7 · E 5

## A. Line typed as a prefix of a longer real line (5)

### case 2 — `/Users/denis.lantsman/src/magenta.nvim/plans/2026-08-02-openai-provider-ground-truth.md`

FAILED:
```
select <<A
- Auth is OAuth 2.0 PKCE against `auth.openai.com`
A
```
WORKED:
```
select /- Auth is OAuth 2\.0 PKCE against/
extend_forward /Recommendation: do this first, build our own login flow second\./
```

### case 14 — `/Users/denis.lantsman/src/magenta.nvim/plans/2026-08-06-edl-fuzzy-select-recovery.md`

FAILED:
```
select <<A
- Score a candidate as normalized Levenshtein distance
A
```
WORKED:
```
select <<A
- Score a candidate as normalized Levenshtein distance...
A
```

### case 16 — `/Users/denis.lantsman/src/glean/plans/2026-08-06-comment-api.md`

FAILED:
```
select <<B
Comment identity. Records have no id.
B
```
WORKED:
```
select <<B
Comment identity. Records have no id. The API derives a stable one:
B
```

### case 20 — `/Users/denis.lantsman/src/magenta.nvim/plans/2026-08-02-openai-provider-ground-truth.md`

FAILED:
```
select <<X
Remaining verification happens in Stage 2: each fixture must replay cleanly through the mock.
X
```
WORKED:
```
select <<X
Remaining verification happens in Stage 2: each fixture must replay cleanly through the mock client.
X
```

### case 28 — `/Users/denis.lantsman/src/glean/context.md`

FAILED:
```
select <<END
- `state.lua` — the persisted ReviewStore.
END
```
WORKED:
```
select <<END
- `state.lua` — the persisted ReviewStore. Keyed by commit sha, sharded one JSON
END
```

## B. Fragment quoted from mid-line or across reflowed prose (4)

### case 8 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/imports/slot.ts`

FAILED:
```
select <<A
 * requiredness speaks only to the parts of its value that must be present:
A
```
WORKED:
```
select <<A
/**
 * A slot's cells and whether the sheet must carry a header for each. A slot's
A
extend_forward <<B
 */
B
```

### case 15 — `/Users/denis.lantsman/src/pkb/README.md`
_paragraph re-quoted from the file_

FAILED:
```
select <<'MATCH'
If you'd rather not run a hook, run `pkb reindex` by hand whenever code lands on the default branch and commit the refreshed `.pkb/index` tree / `pkb-state.toml`. Because those files then need their own commit, the index is delivered one commit _behind_ the code it represents — the index commit on `main` reflects its parent. This lag is harmless: agents orient on the last indexed commit and read files for anything newer.
MATCH
```
WORKED:
```
select <<'MATCH'
Run the pkb binary from anywhere inside the git repository. PKB discovers the repo root based on cwd, and runs against the `.pkb/index` mirror tree at the repo root (rebuilding the local `.pkb/cache.db` as needed).
MATCH
```

### case 18 — `/Users/denis.lantsman/src/glean/plans/2026-08-06-comment-api.md`

FAILED:
```
select <<B
...snippets for each entry point plus the "no session open" recovery.- Tests: manual — run each snippet from the skill against a live review and confirm the documented shape matches the actual return values.
B
```
WORKED:
```
select <<C
  - Comment ids are stable across two calls...
C
extend_forward <<C
  - A session resolves by its id string, by its full buffer name, and by buffer number.
C
```

### case 19 — `/Users/denis.lantsman/src/glean/plans/2026-08-06-comment-api.md`

FAILED:
```
select <<B
...the API takes `{ session, id }` and errors on an unknown id — never a silent no-op....
B
```
WORKED:
```
select <<B
Comment identity. Comments are persisted...
B
narrow /the API takes `{ session, id }` and errors on an unknown id/
```

## C. Oversized multi-line block written from memory (8)

### case 0 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/imports/created-persons.test.ts`

FAILED:
```
select <<END
const NAME = "" as FieldId;
const EMAIL = "email" as FieldId;
const schema = { matchingFields: personCreateFields, fields: [] };
function person(given: string, family: string, email: string) {
  return {
    values: {
      [NAME]: {
        type: "name",
        value: { given_name: given, family_name: family },
      } as TypedValue,
      [EMAIL]: { type: "email", value: email } as TypedValue,
    },
    custom_fields: {},
  };
}
END
```
WORKED:
```
select <<END
const NAME = "" as FieldId;
END
extend_forward <<END
}
END
```

### case 5 — `/Users/denis.lantsman/src/shuck/doc/shuck.txt`

FAILED:
```
select <<E
      default_cmd     = "rg -H --no-heading --vimgrep ",
      max_render      = 200,
    require("shuck").toggle({ cmd = "fd -t f " })
<
The `cmd` option picks the command shuck starts from: the prompt is seeded with
it and <C-u> resets to it. Each base command remembers its own last accepted
search, and toggling an open shuck with a different base rebases it in place
instead of closing it.
      max_results     = 10000,
E
```
WORKED:
```
select <<E
      default_cmd     = "rg -H --no-heading --vimgrep ",
E
extend_forward <<E
instead of closing it.
E
```

### case 6 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/events/event-write.ts`

FAILED:
```
select <<END
function parseMutableParams(
  raw: unknown,
  requireCoreFields: boolean,
): Partial<EventMutableParams> {
  const body = parseObject(raw, "event parameters");
  const name = optionalString(body, "name", false);
  const startsAt = optionalString(body, "starts_at", false);
  const endsAt = optionalString(body, "ends_at", false);
  if (requireCoreFields && name === undefined) badRequest("name is required");
  if (requireCoreFields && startsAt === undefined)
    badRequest("starts_at is required");
  if (requireCoreFields && endsAt === undefined)
    badRequest("ends_at is required");
  const eventGroupId = optionalString(body, "event_group_id", true);
  const rosterId = optionalString(body, "roster_id", true);
END
```
WORKED:
```
select <<END
function requiredString(value: unknown, field: string): string {
END
extend_forward <<END
function parseMutableParams(
END
```

### case 7 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/events/event-write.ts`

FAILED:
```
select <<END
  raw: unknown,
  requireCoreFields: boolean,
): Partial<EventMutableParams> {
  const body = parseObject(raw, "event parameters");
  const name = optionalString(body, "name", false);
  const startsAt = optionalString(body, "starts_at", false);
  const endsAt = optionalString(body, "ends_at", false);
  if (requireCoreFields && name === undefined) badRequest("name is required");
  if (requireCoreFields && startsAt === undefined)
    badRequest("starts_at is required");
  if (requireCoreFields && endsAt === undefined)
    badRequest("ends_at is required");
  const eventGroupId = optionalString(body, "event_group_id", true);
  const rosterId = optionalString(body, "roster_id", true);
END
```
WORKED:
```
select <<END
  raw: unknown,
  requireCoreFields: boolean,
): Partial<EventMutableParams> {
  const body = parseObject(raw, "event parameters");
  const name = optionalString(body, "name", false);
  const startsAt = optionalString(body, "starts_at", false);
  const endsAt = optionalString(body, "ends_at", false);
  if (requireCoreFields && name === undefined) badRequest("name is required");
  if (requireCoreFields && startsAt === undefined)
    badRequest("starts_at is required");
  if (requireCoreFields && endsAt === undefined)
    badRequest("ends_at is required");
END
select <<END
  const eventGroupId = optionalString(body, "event_group_id", true);
  const rosterId = optionalString(body, "roster_id", true);
END
```

### case 9 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/imports/created-persons.test.ts`

FAILED:
```
select <<D
  custom_fields: {},
  };
}
D
```
WORKED:
```
select <<D
describe("created persons", () => {
D
select <<D
  it("calls two created persons sharing a key ambiguous", () => {
D
```

### case 10 — `/Users/denis.lantsman/src/magenta.nvim/node/core/src/providers/mock-openai-client.ts`

FAILED:
```
select <<END
    this._resolved = true;
    this.pushEvent({
      type: "response.completed",
      response: mockResponse(output, usage),
    });
    this.close();
  }
  finishResponse(
END
```
WORKED:
```
select <<END
  finishResponseWithOutput(
END
extend_forward <<END
    this._resolved = true;
END
```

### case 13 — `/Users/denis.lantsman/src/magenta.nvim/node/core/src/providers/openai-agent.test.ts`

FAILED:
```
select <<END
    expect(agent.getState().messages).toHaveLength(2);
  });
  it("streams text, emits didUpdate, and stops with usage", async () => {
END
```
WORKED:
```
select <<END
    stream.streamWebSearchCall("ws_1", "denis lantsman");
END
```

### case 26 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/events/routes.ts`

FAILED:
```
select <<I
      const rows = await listResponsesByEvent.run(
        {
          eventId,
          rosterId: roster?.rosterId ?? null,
I
```
WORKED:
```
select <<A
      const PAGE_SIZE = 20;
A
select_prev <<G
      ctx.require("response:read", { kind: "response", orgId: ctx.orgId });
G
```

## D. Target does not exist — invented or stale (7)

### case 1 — `/Users/denis.lantsman/src/magenta.nvim/plans/2026-08-02-openai-provider-ground-truth.md`
_different location_

FAILED:
```
select <<D
- Substage A2: our own PKCE login
D
```
WORKED:
```
select <<A
## Stage A — ChatGPT subscription auth
A
extend_forward <<B
- Invariant: tokens are never logged and never written into fixtures; capture must scrub `Authorization`, `chatgpt-account-id`, and `id_token`.
B
```

### case 21 — `/Users/denis.lantsman/src/glean/lua/glean/init.lua`
_different location_

FAILED:
```
select <<C
          emit("  > " .. part, ctarget, "GleanComment")
C
```
WORKED:
```
select <<A
    local lead = c.outdated and "> (outdated) " or "> "
A
```

### case 22 — `/Users/denis.lantsman/src/glean/lua/glean/init.lua`
_abandoned, no retry_

FAILED:
```
select <<'S1'
  return self:ids_all_seen(ids)
end
-- Overall unreviewed work in the current scope. Lines count individually, while
S1
select <<'S2'
  h.assert_true("render: shows hunk header", joined:find("@@", 1, true) ~= nil)
end
-- The top row summarizes remaining review work and updates as lines are marked.
S2
select <<'S3'
inside an otherwise-unseen hunk collapses into a `✓ marked N lines` marker row.
The top row reports the remaining unreviewed files, hunks, additions, and
S3
```
WORKED:
```
(nothing — abandoned)
```

### case 23 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/responses/response-write.ts`
_different comment_

FAILED:
```
select <<A
/**
 * Which of a version's fields a submission is answering.
A
```
WORKED:
```
select <<A
/** Which field failed, and why — the shape the wire's `validation` error carries. */
A
```

### case 24 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/survey-instances/routes.ts`
_different import_

FAILED:
```
select <<J
import { parseSchema } from "../survey-templates/routes.ts";
J
```
WORKED:
```
select <<I
import {
  projectPersonIdentity,
  validateQuestionFields,
  validateSubmission,
} from "../persons/validate-custom-fields.ts";
I
```

### case 25 — `/Users/denis.lantsman/src/gatherus/survey-import-validation/packages/backend/persons/validate-custom-fields.ts`
_different file_

FAILED:
```
select <<C
/**
 * Validate a public-survey submission's `answers` map against a schema.
C
```
WORKED:
```
file `packages/backend/models/responses/index.ts`
select <<A
import type { NormalizedResponseCreateParams } from "../../responses/response-write.ts";
import { plainAnswers } from "../../responses/response-write.ts";
A
```

### case 27 — `/Users/denis.lantsman/src/gatherus/main/packages/backend/rosters/routes.ts`
_different import_

FAILED:
```
select <<END
import { getRoster } from "../models/rosters/index.ts";
END
```
WORKED:
```
select <<END
import { withClient } from "../db/connect.ts";
END
```

## E. Regex-specific mistake (5)

### case 3 — `/Users/denis.lantsman/src/shuck/lua/shuck/init.lua`

FAILED:
```
select_multiple /^    buffer = prompt_buf,$/
```
WORKED:
```
select_multiple /buffer = prompt_buf,/
```

### case 4 — `/Users/denis.lantsman/src/shuck/lua/shuck/init_test.lua`

FAILED:
```
select_multiple /work \.\. "\/b.txt"\)$/
```
WORKED:
```
select_multiple /work \.\. "\/b\.txt"\)/
```

### case 11 — `/Users/denis.lantsman/src/glean/lua/glean/init_test.lua`

FAILED:
```
select_multiple /and line:match\("\^%%\[%%d\+%%\] (first note|second note)\$"\)/
```
WORKED:
```
select <<A
      and line:match("^%[%d+%] first note$")
A
select <<B
      and line:match("^%[%d+%] second note$")
B
```

### case 12 — `/Users/denis.lantsman/src/magenta.nvim/node/core/src/providers/openai.ts`
_different import_

FAILED:
```
select /^import OpenAI from "openai";$/
```
WORKED:
```
select <<END
import { getRetryDelay, MAX_RETRY_DURATION } from "./anthropic-agent.ts";
END
```

### case 17 — `/Users/denis.lantsman/src/glean/plans/2026-08-06-comment-api.md`

FAILED:
```
select /^Session lookup\. .*$/
```
WORKED:
```
select <<A
Session lookup. `init.lua` gains a minimal accessor...
A
```

